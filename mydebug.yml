---
- hosts: 172.31.36.37
  become: yes
  tasks:
  - name: i want to execute linux command
    command: hostname -f
    register: myResult
  - name: i want to display my first task myResult
    debug:
       msg: "{{myResult}}"
